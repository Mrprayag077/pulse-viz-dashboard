const Navbar = () => (
  <nav className="w-full bg-gray-800 text-white py-4 px-8 flex justify-between">
    <h1 className="text-lg font-semibold">Analytics Dashboard</h1>
    <span>User</span>
  </nav>
);

export default Navbar;
